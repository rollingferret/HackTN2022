"""Test seeder logic."""

# import re
# from geopy.geocoders import Nominatim
# app = Nominatim(user_agent='day_care_finder')

# raw_address = '17580 CLAY CO. HWY, RED BOILING SPRING TN 37150'
# geo_addy = app.geocode(raw_address, timeout=10)
# split_up = app.geocode(raw_address.split(', ')[1]).raw
# split_up = raw_address.split(', ')
# split_up = re.match(r'(.*\s)([0-9]{1,}$)', raw_address).group(2)
# location = geo_addy.raw if geo_addy else app.geocode(split_up).raw

# print(split_up)
# print(geo_addy)
# print(location['lat'], location['lon'])
# print(geo_addy)